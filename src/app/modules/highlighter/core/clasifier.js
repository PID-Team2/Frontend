export const clasifier = (word) => {

    let result  = {
        word : '',
        clasification : '',
        error : true
    }

    if( typeof word !== "string")
        return result;

    
    return result;
}